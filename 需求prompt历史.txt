# 第一次更新
comfyui的启动方式(comfy) zdx@bm-cn-huhehaote2-ix-ey8vsp1i:/mnt/data0/gyb/comfyui_api/ComfyUI$ python main.py --listen 0.0.0.0 --port 18001
将/mnt/data0/gyb/comfyui_api/image_edit.json从comfyui分离出来成为一个独立服务api
# 第二次更新
兼容openai格式,
请求体:
{
    "image1":"https://imgloc.com/image/yil12C",
    "prompt": "人物手部动作自然大方，绿色背景保持稳定不变色，优化画面清晰度",
    "steps":4,
    "width":1280,
    "height":720
}
备注:
    "width":1280,
    "height":720
这两个参数没有传递时候自动识别图像尺寸大小来决定传出图像的尺寸,
你自己直接用python重构,模型在
/mnt/data0/gyb/comfyui_api/ComfyUI/models/checkpoints/Qwen-Rapid-AIO-SFW-v23.safetensors
不要依赖comfyui的启动
# 第三次更新
返回图片url
# 第四次更新
支持在cuda3,4,5,6,7上实现五并发,并且模型热加载,并且优化代码
# 第五次更新
"seed":字段不需要传入,需要自动生成随机数,不能一样.
# 第六次更新
1.接受任务后能自动分发给空闲的cuda,
2.增加异常处理机制,如果其中某一cuda显存oom了,重新启动该cuda服务,并且把他的任务转让给其他cuda.
# 第七次更新
问两个问题
1.比如说传进来50个任务,你能给他们排队吗,
2.任务请求和结果能保证一致吗
# 第八次更新
效率好低,模型热加载为什么还要24s,不能5s吗
# 第九次更新
comfyui的地址变为了/mnt/data0/AIGC/story/comfyui_main
# 第十次更新
改为cuda1, 2各三并发 (共6个worker)
#第十一次更新
启动速度可以降低保证不要突然IO的增加把服务器卡死,
（已在 api_service.py 中为每个 worker 启动增加 15 秒延迟）
#第十二次更新
更新 requirements.txt，包含 fastapi, uvicorn, requests, pillow, numpy, torch, safetensors, pydantic 等依赖。
#第十三次更新
1. 修复图像下载 URL 包含空格或反引号导致的 400 错误。
2. GPU 并发配置降低为：cuda0(1), cuda1(2), cuda2(2)，共 5 并发。
3. 支持通过 .env 文件中的 GPU_CONCURRENCY_CONFIG 变量便捷切换每个 GPU 的并发数。
#第十四次更新
修复 RuntimeError: Can't call numpy() on Tensor that requires grad。在 worker.py 中增加 torch.no_grad() 上下文并使用 .detach() 处理输出 tensor。
#第十五次更新
生成并完善详细的 README.md，包含项目概述、核心特性、安装配置、运行指南及 API 使用说明。

